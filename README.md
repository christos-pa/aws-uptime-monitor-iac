# aws-uptime-monitor-iac
Production-style AWS uptime monitoring system built with Terraform. Provisions a web server and automated monitoring pipeline (Lambda + EventBridge + SNS) with remote state and outage simulation.
